{
    "name": "bvnsp-signin-twilio",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "./node_modules/.bin/webpack",
        "prestart": "npm run build",
        "start": "twilio-run",
        "debug": "npm run build && twilio-run --inspect",
        "deploy": "twilio-run deploy"
    },
    "dependencies": {
        "@twilio-labs/serverless-runtime-types": "^2.2.3",
        "@twilio/runtime-handler": "1.3.0",
        "googleapis": "^118.0.0"
    },
    "devDependencies": {
        "assert": "^2.0.0",
        "buffer": "^6.0.3",
        "crypto-browserify": "^3.12.0",
        "https-browserify": "^1.0.0",
        "os-browserify": "^0.3.0",
        "process": "^0.11.10",
        "stream-browserify": "^3.0.0",
        "stream-http": "^3.2.0",
        "ts-loader": "^9.4.2",
        "twilio-run": "^3.5.0",
        "typescript": "^5.0.4",
        "url": "^0.11.0",
        "webpack": "^5.83.1",
        "webpack-cli": "^5.1.1"
    },
    "engines": {
        "node": "16"
    },
    "twilioserverless": {
        "runtime": "node16"
    }
}